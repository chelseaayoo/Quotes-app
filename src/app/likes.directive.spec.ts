import { LikesDirective } from './likes.directive';

describe('LikesDirective', () => {
  it('should create an instance', () => {
    const directive = new LikesDirective();
    expect(directive).toBeTruthy();
  });
});
